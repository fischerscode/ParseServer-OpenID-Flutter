name: parseserver_openid_flutter
description: A package that adds openid authentification to the ParseServer Flutter package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  parse_server_sdk_flutter: ^2.1.0
  openid_client: ^0.3.1
  url_launcher: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter