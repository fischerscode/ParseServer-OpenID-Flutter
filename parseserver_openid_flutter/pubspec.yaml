name: parseserver_openid_flutter
description: A package that adds openid authentification to the ParseServer Flutter package.
version: 0.0.3
homepage: https://github.com/fischerscode/ParseServer-OpenID-Flutter

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  parse_server_sdk_flutter: ^2.1.0
  oauth2: ^1.6.3
  url_launcher: ^6.0.2

  shared_preferences: ^0.5.12+4

  uni_links: ^0.4.0

  rxdart: ">=0.24.1 <1.0.0"

  http: ">=0.12.0 <1.0.0"


dev_dependencies:
  flutter_test:
    sdk: flutter